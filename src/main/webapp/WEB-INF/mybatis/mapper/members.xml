<?xml version="1.0" encoding="euc-kr" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.sabangpalbang.dao.MemberDao">
	<insert id="insertByMember" parameterType="member">
		insert into member
		(member_email, member_pw, member_name, member_phone, member_nickname, member_enable, member_authority, join_date, zipcode, roadaddress, detailaddress)
		values
		(#{member_email}, #{member_pw}, #{member_name}, #{member_phone}, #{member_nickname}, #{member_enable}, #{member_authority}, #{join_date}, #{zipcode}, #{roadaddress}, #{detailaddress})
	</insert>
	
	
	<select id="selectByMember" parameterType="string" resultType="member">
  		select member_email, member_pw, member_name, member_phone, member_nickname, zipcode, roadaddress, detailaddress
  		from member
  		where member_email=#{member_email}
  	</select>
  	
  	<select id="selectByPayMember" parameterType="string" resultType="member">
  		select member_name, zipcode, roadaddress, detailaddress, member_phone
  		from member
  		where member_email=#{member_email}
  	</select>
  	
	<update id="updateByMember" parameterType="member">
		update member set
		member_pw=#{member_pw},
		member_phone=#{member_phone},
		zipcode=#{zipcode},
		roadaddress=#{roadaddress},
		detailaddress=#{detailaddress},
		member_nickname=#{member_nickname}
		where member_email=#{member_email}
	</update>
	
	<delete id="deleteByMember" parameterType="string">
		delete from member
		where member_email=#{member_email}
	</delete>
	
	
	
	<!-- 이메일 찾기, 비번 찾기는 낼 수업때 배우고 난 후 -->
</mapper>

